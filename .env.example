# Backend environment configuration for HRMS Lite

# Prefer PostgreSQL in production, example:
# postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/hrms_lite

# Comma-separated list of frontend origins, e.g. http://localhost:5173,https://your-frontend.app
# Leave empty to allow all origins in development (not recommended for production).
BACKEND_CORS_ORIGINS=

